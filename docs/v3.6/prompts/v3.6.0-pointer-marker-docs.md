# v3.6.0 Pointer Marker Docs

Implement the v3.6.0 slice only.

This file is one v3.6 implementation slice.

If invoked from a broad instruction such as "implement the next slice", first confirm this file matches the current target or the first unchecked allowed slice in `docs/v3.6/ImplementationState.md`.

## Goal

Rewrite and maintain the v3.6 planning scaffold for physical pointer markers.

This slice is docs-only unless the existing project requires a generated index or manifest update for docs discovery.

## Requirements

- Update `docs/v3.6/ImplementationState.md`.
- Update `docs/v3.6/ReleasePlan.md`.
- Update `docs/v3.6/prompts/README.md`.
- Update prompt files for the v3.6 slices.
- Establish `realism.pointer_markers` as the config namespace.
- Define pointer markers as instrument-realism features, not statistical overlays.
- State that pointer markers sample the final rendered indicator position for the gauge family.
- State that pointer markers do not sample source values, logs, exports, clean mapped values, or pre-realism values.
- Lock simple boolean marker config for `max`, `min`, and `average`.
- Reject long-form marker object config and unknown marker keys.
- Document no-`window` daily local reset mode for min/max.
- Document optional rolling-window min/max history when `window` is present.
- Document `average` as a 10 second highly damped physical pointer, not a statistical average.
- Document radial and bar support.
- Document marker PNG assets for radial and bar gauges.
- Document marker rendering above the live needle/bar and below overlay/glass/bezel/frame layers.
- Mark the older future stat-marker note as superseded by v3.6 pointer markers where appropriate.

## Do not

- Do not implement code in this slice unless required by docs infrastructure.
- Do not add mathematical average semantics.
- Do not add persistence semantics.
- Do not add long-form config support.
- Do not change v3.5 docs except for cross-reference fixes that are strictly necessary.

## Completion

Update `docs/v3.6/ImplementationState.md` to mark v3.6.0 complete and advance `Current target` to v3.6.1 only after the v3.6 docs and prompts are internally consistent.
