# v3.6.7 Pointer Marker Tests / Previews / Docs

Implement the v3.6.7 slice only.

This file is one v3.6 implementation slice.

If invoked from a broad instruction such as "implement the next slice", first confirm this file matches the current target or the first unchecked allowed slice in `docs/v3.6/ImplementationState.md`.

Do not implement this slice if v3.6.6 is still unchecked.

## Goal

Finalise the v3.6 pointer marker work and confirm the release stayed scoped to pointer markers only.

This is primarily a verification, preview, and documentation checkpoint slice.

## Required verification

Confirm v3.6 has coverage or visual checks for:

- simple boolean config validation;
- rejected long-form config;
- rejected unknown keys;
- rejected `pointer_markers: true`;
- final rendered indicator position sampling;
- daily local reset when `window` is absent;
- rolling-window min/max history when `window` is present;
- fixed 10 second average marker time constant;
- no statistical average calculation;
- radial marker rendering above the live needle;
- bar marker rendering above the live bar/fill/indicator;
- marker rendering below overlay/glass/bezel/frame layers;
- radial marker PNG asset usage;
- bar marker PNG asset usage;
- no mutation of source values, logs, exports, configured ranges, or input data.

## Preview requirements

At minimum, v3.6 should have:

- one radial preview showing min, max, and average pointer markers;
- one bar preview showing min, max, and average pointer markers;
- a sweep or changing input that makes marker behaviour easy to judge by eye.

## Documentation requirements

- Review `docs/v3.6/ImplementationState.md` and `docs/v3.6/ReleasePlan.md`.
- Confirm the old future-stat-marker model is not accidentally still described as the v3.6 behaviour.
- Move any newly discovered non-marker follow-up ideas to v3.7+ docs or a separate issue/PR.

## Do not

- Do not implement backlog items during this checkpoint unless the user explicitly asks for a separate follow-up release.
- Do not mix mathematical statistics overlays into `realism.pointer_markers`.
- Do not add persistence casually.
- Do not add odometer backlash or other non-marker realism work to v3.6.
- Do not promote new v3.6 slices unless the user explicitly changes the release scope.

## Completion

Update `docs/v3.6/ImplementationState.md` to mark v3.6.7 complete and set `Current target` to `none` only after the pointer marker docs, tests, previews, and relevant checks are complete.
