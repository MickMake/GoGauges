# v3.3.0 - renderer checkpoint planning

## Goal

Create the v3.3 planning scaffold for the renderer spike and move the active baseline fixture path toward `examples/`.

## Required context

Read:

- `docs/v3.3/ReleasePlan.md`
- `docs/v3.3/ImplementationState.md`
- `docs/v3.3/BaselineDashboardVerification.md`
- `examples/baseline-dashboard.yaml`

## Scope

- Create `docs/v3.3/` planning docs.
- Create `docs/v3.3/prompts/` with intent-named prompt files.
- Keep prompt filenames prefixed with the full version number.
- Use branches prefixed with the full version number.
- Make `examples/baseline-dashboard.yaml` the canonical reusable baseline config.
- Keep Fyne as the default renderer in the plan.
- Record that Ebiten is experimental until measured.

## Acceptance criteria

- `docs/v3.3/ReleasePlan.md` exists.
- `docs/v3.3/ImplementationState.md` exists.
- `docs/v3.3/BaselineDashboardVerification.md` exists.
- `docs/v3.3/prompts/README.md` exists.
- Prompt files exist for `v3.3.0` through `v3.3.3`.
- `examples/baseline-dashboard.yaml` exists and is the documented command target.
- `CHANGES.md` is updated.
- No renderer implementation code is changed.

## Do not

- Do not implement Ebiten in this slice.
- Do not remove Fyne.
- Do not redesign gauge packages.
- Do not change sensor ownership rules.
