# GoDriveLog v3.4.10 — Dashboard CLI command tree

You are working in the GoDriveLog repository.

Create the v3.4.10 dashboard CLI command-tree slice.

This is a thin CLI remapping slice. It moves the existing flat flag dashboard entry points into dashboard-scoped subcommands so the public command tree is created together.

The existing runtime, renderer selection, config loading, validation, harness, selected-vehicle dashboard behaviour, and generated-example machinery already exist. Do not invent replacement systems.

Keep this slice to command routing and wiring. Do not implement the compact overview or gauge-aware harness sweep here.

## CLI remapping rule

Map the existing `cmd/GoDriveLog/main_ebiten.go` flat flag behaviours into dashboard command forms.

Most implementation work should stay in `cmd/GoDriveLog/main_ebiten.go` unless a tiny helper extraction is needed to avoid duplicate routing code.

Any extracted helper must preserve the same underlying behaviour already reached by the current flags.

Do not create new runtime packages, renderer abstractions, config schemas, validation engines, harness engines, or example-generation systems as part of this slice.

## Command forms to map in this slice

Map these command forms to existing behaviour:

```text
GoDriveLog dashboard run [vehicle-id]
  [--config <config-file>]
  [--renderer ebiten]

GoDriveLog dashboard harness [vehicle-id]
  [--config <config-file>]
  [--pattern sweep]
  [--interval 50ms]
  [--duration 60s]
  [--renderer ebiten]

GoDriveLog dashboard examples
  --output <directory>
  [--config <config-file>]
  [--vehicle <vehicle-id>]
  [--theme framework-smoke]
  [--force]

GoDriveLog dashboard validate [config-file]
GoDriveLog dashboard validate [--config <config-file>]
```

Also establish the `dashboard` command namespace and help output needed for these commands.

Do not implement bare `GoDriveLog dashboard [--config <config-file>]` compact overview in this slice. That remains v3.4.11.

Do not add `dashboard preview` in this slice.

Do not add compatibility or migration behaviour for any earlier flat flag shape. Implement only the dashboard command tree described in the active v3.4 docs.

## Command routing table

| New command form | Existing flat flag path or machinery being remapped | Existing code path to reuse |
|---|---|---|
| `GoDriveLog dashboard run [vehicle-id]` | Default run path when `--harness=false`; uses existing `--config`, `--vehicle`, `--renderer`, and current runtime duration handling if preserved. | Existing `runV3EbitenCommand(configPath, vehicleID, duration)` path. |
| `GoDriveLog dashboard harness [vehicle-id]` | Existing `--harness=true` path plus `--config`, `--vehicle`, `--pattern`, `--interval`, `--duration`, and `--renderer`. | Existing `runV3EbitenHarnessCommand(configPath, vehicleID, pattern, interval, duration)` path. |
| `GoDriveLog dashboard validate [config-file]` | Existing config load and validation behaviour, reached through a command instead of flat flags. | Existing config parsing/validation helpers; do not create a replacement validator. |
| `GoDriveLog dashboard validate --config <config-file>` | Existing `--config` file selection plus existing config validation behaviour. | Existing config parsing/validation helpers; do not create a replacement validator. |
| `GoDriveLog dashboard examples --output <directory>` | Existing generated example asset machinery/scripts, plus existing `--config` and `--vehicle` concepts where relevant. | Existing generated-example helpers/scripts; do not build a duplicate generator. |

`--vehicle` becomes positional `[vehicle-id]` for `dashboard run` and `dashboard harness`.

`--vehicle` remains a flag for `dashboard examples`.

`--harness` becomes the `dashboard harness` command name.

`--v3` does not become a dashboard flag. The `dashboard` command tree implies the active v3 dashboard path.

## Deferred command slices

The remaining dashboard CLI tail is split into later slices:

| Version | Slice | Deferred work |
|---|---|---|
| v3.4.11 | dashboard overview | Bare `GoDriveLog dashboard [--config <config-file>]` prints a compact config overview by reusing existing config structures. |
| v3.4.12 | gauge-aware harness sweep | `dashboard harness --pattern sweep` becomes gauge-aware synthetic input after the harness command exists. |

## Defaults and config resolution

`--config` is optional for commands that accept it.

When a positional config file or `--config` is supplied, load exactly that file and bypass config discovery.

If both a positional config file and `--config` are supplied to `dashboard validate`, return an error.

Do not default to `config.example.yaml`.

Candidate config filenames are:

```text
godrivelog.yaml
godrivelog.yml
dashboard.yaml
dashboard.yml
config.yaml
config.yml
```

When no config file is supplied, search config files from:

```text
current working directory
/etc/godrivelog recursively
```

Directory traversal must be deterministic:

- sort each directory's entries alphabetically before evaluating them;
- search the current working directory non-recursively;
- search `/etc/godrivelog` recursively;
- evaluate only candidate config filenames.

### No vehicle specified

For every candidate config file in search order:

- if the config defines exactly one vehicle, use that config and that vehicle;
- if the config defines multiple vehicles, stop and return an error requiring a vehicle ID.

Do not continue past the first valid multi-vehicle config when no vehicle ID was supplied.

The multi-vehicle error must include the config path and available vehicle IDs.

### Vehicle specified

For every candidate config file in search order:

- if the config defines the requested vehicle ID, use that config and that vehicle;
- otherwise continue searching.

The matching vehicle may be in a single-vehicle or multi-vehicle config.

If no searched config contains the requested vehicle ID, return a useful error listing the searched config files and the vehicle IDs found in each valid config.

### Explicit config plus vehicle

When an explicit config file is supplied and a vehicle ID is supplied, do not continue searching other files. Load that config only. If it does not contain the requested vehicle ID, return an error for that file.

## Renderer selection

`--renderer` is optional and defaults to `ebiten`.

The only supported active renderer remains `ebiten`.

Do not add renderer aliases or resurrect old renderer paths.

## Run command

`dashboard run` starts the active dashboard runtime for the selected vehicle.

It should call the same v3 selected-vehicle runtime currently used by the active Ebiten command path.

Do not change dashboard rendering semantics.

## Harness command

`dashboard harness` runs the dashboard harness for the selected vehicle through the dashboard command tree by reusing the existing harness path.

Keep existing pattern options for now. `sweep` remains the default.

Do not make `sweep` gauge-aware in this slice. The gauge-aware sweep behaviour is v3.4.12.

Do not over-expand `heartbeat` or `fixed`.

## Examples command

`dashboard examples` generates a deterministic example dashboard by wrapping the existing generated-example machinery.

`--output <directory>` is required.

The output path is the dashboard root directory:

```text
<output>/
  dashboard.yaml
  assets/
    panel/
    gauges/
```

Rules:

- create `<output>` if it does not exist;
- write `dashboard.yaml` directly under `<output>`;
- write `assets/` directly under `<output>`;
- do not create a theme-named subdirectory under `<output>`;
- all internal dashboard and gauge-package paths must be relative to `<output>` so the generated dashboard directory can be moved anywhere;
- if `<output>` exists and is empty, use it;
- if `<output>` exists and is non-empty, interactive terminals may prompt unless `--force` is supplied;
- if `<output>` exists and is non-empty in non-interactive use, fail unless `--force` is supplied;
- if `<output>` exists and is a file, return an error.

Keep generated example output deterministic and local. Do not use remote image generation, stock art downloads, or hand-edited opaque PNGs as source of truth.

The implementation may reuse or wrap the existing generated example asset machinery, but the public CLI shape should remain `dashboard examples`.

## Validate command

`dashboard validate` validates the resolved config using existing config validation behaviour.

Allowed forms:

```text
GoDriveLog dashboard validate
GoDriveLog dashboard validate ./config.yaml
GoDriveLog dashboard validate --config ./config.yaml
```

Rules:

- If no config is supplied, use normal config discovery.
- If a positional config is supplied, validate that file.
- If `--config` is supplied, validate that file.
- If both positional config and `--config` are supplied, return an error.

## Help output

Add help output coverage for:

```text
GoDriveLog dashboard --help
GoDriveLog dashboard run --help
GoDriveLog dashboard harness --help
GoDriveLog dashboard examples --help
GoDriveLog dashboard validate --help
```

The help output should include:

- command purpose;
- positional arguments;
- supported flags;
- defaults where useful;
- one short example where practical.

## Documentation and cleanup

Update:

- `CHANGES.md`
- `docs/v3.4/ReleasePlan.md`
- `docs/v3.4/ImplementationState.md`
- any command examples in README/docs that imply another active dashboard CLI shape

Keep historical command examples historical. Do not rewrite archived docs unless they are actively misleading current users.

## Validation

Run:

```text
gofmt on changed Go files
go test ./...
```

If this remains a docs-only planning slice, state that no Go tests were required and explain why.

Finish by summarising:

- files changed;
- command routing implemented or documented;
- validation commands run;
- intentionally deferred CLI behaviours.