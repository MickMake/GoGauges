# GoDriveLog v3.4.8 — Neon Grid Dashboard

You are working in the GoDriveLog repository.

Create the v3.4.8 generated example dashboard:

Dashboard name:
neon-grid

Generated dashboard location:
examples/neon-grid/

Portable dashboard contract:
A generated dashboard example is a self-contained directory:

examples/<dashboard>/
  dashboard.yaml
  assets/

All dashboard-local references must be relative to that dashboard directory.
All gauge-package-local references must be relative to the gauge package.
The generated dashboard portability contract test must pass.

Do not redesign the dashboard layout rules.
Existing generated dashboards must remain valid under the same contract.

Design goal:
Create a neon-grid dashboard with a dark synthetic grid look, inspired by retro-futuristic instrument panels.

Use local procedural generation only.
No remote image generation.
No stock image downloads.
No hand-edited PNGs as source of truth.

The dashboard should include:
- a neon-style background panel;
- a speed display;
- an RPM gauge;
- a fuel or temperature-style bar;
- at least one warning/indicator gauge;
- all required generated assets under examples/neon-grid/assets/.

Gauge packages:
Each generated gauge package must be complete and portable.
Each gauge package must include its own gauge.yaml beside its required assets.

Paths:
Dashboard YAML must use relative paths such as:
gauge: assets/gauges/<gauge_name>

Gauge YAML files must use package-local relative paths for their own image assets.

Validation:
Run gofmt on changed Go files.

Run:
go test ./scripts/generate-example-assets
go test ./internal/dashboard/harness
go test ./...

Finish by summarising:
- files changed;
- generated dashboard path;
- validation commands run;
- whether the portability contract test passes.
