# GoDriveLog v3.4.12 — Gauge-aware harness sweep

You are working in the GoDriveLog repository.

Create the v3.4.12 gauge-aware harness sweep slice.

This slice runs after v3.4.10 has established the full dashboard CLI command tree, including `dashboard harness`, and after v3.4.11 has added the compact dashboard overview.

This is harness behaviour refinement only. Do not create replacement runtime, renderer, harness, config, validation, or example-generation systems.

Do not add new renderer behaviour, gauge package semantics, CLI command forms, or generated example output in this slice.

## Existing command shape

Use the `dashboard harness` command already created in v3.4.10:

```text
GoDriveLog dashboard harness [vehicle-id]
  [--config <config-file>]
  [--pattern sweep]
  [--interval 50ms]
  [--duration 60s]
  [--renderer ebiten]
```

Do not add `dashboard preview` in this slice.

Do not change `dashboard examples` in this slice.

## Harness command

`dashboard harness` already routes to the existing harness path.

Keep existing pattern options for now. `sweep` remains the default.

The only intended harness behaviour change in this slice is that `sweep` becomes gauge-aware synthetic input. It should drive each gauge in a way that matches the gauge behaviour instead of treating every gauge as the same numeric sweep.

Gauge-aware sweep behaviour:

| Gauge type | Sweep behaviour |
|---|---|
| `odometer` | Let `n = 0`; increment from `n - 20` to `n + 20` for 5 seconds, then `n + 20` to `n + 30` for 5 seconds. |
| `numeric` | Same as `odometer`. |
| `radial` | Keep the existing sweep style. |
| `indicator` | Flash on/off for 5 seconds with a 1s duty cycle, then flash on/off for 5 seconds with a 250ms duty cycle. |
| `bar` | Heartbeat pulse at 90 bpm. |
| `segmented` | Same input shape as `radial`. |

Do not over-expand `heartbeat` or `fixed` yet. If they look odd for some gauge types, leave that for a later scope decision.

## Help output

Update help output coverage only if the gauge-aware sweep behaviour changes documented `dashboard harness --help` text.

Do not add help tests for new commands, because this slice should not add new commands.

## Documentation and cleanup

Update:

- `CHANGES.md`
- `docs/v3.4/ImplementationState.md`
- any command examples in README/docs that should mention gauge-aware harness sweep behaviour

## Validation

Run:

```text
gofmt on changed Go files
go test ./...
```

Finish by summarising:

- files changed;
- gauge-aware sweep behaviour implemented;
- validation commands run;
- intentionally deferred CLI behaviours.