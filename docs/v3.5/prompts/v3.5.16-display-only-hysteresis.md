# v3.5.16 Radial Display-Only Hysteresis

Implement the v3.5.16 slice only.

This file is one v3.5 implementation slice.

If invoked from a broad instruction such as "implement the next slice", first confirm this file matches the first unchecked slice in `docs/v3.5/ImplementationState.md`.

Do not implement this slice if an earlier slice is still unchecked.

## Goal

Add direction-dependent displayed offset for radial gauges without changing source values.

## Requirements

- Hysteresis is display-only.
- Apply this slice to radial gauges only; bar hysteresis is split into v3.5.20.
- Source values, logs, exported values, and configured ranges must remain unchanged.
- Direction should affect only the displayed radial angle within configured bounds.
- Add tests for rising approach, falling approach, disabled/default behaviour, unsupported gauge types, and unchanged source value.
- Add a single-feature visual inspection fixture.

## Do not

- Do not apply hysteresis to raw sensor/source values.
- Do not add bar hysteresis in this slice.
- Do not add random drift.
- Do not add idle animation.

## Completion

Update `docs/v3.5/ImplementationState.md` to mark v3.5.16 complete and set the next target to v3.5.19.
