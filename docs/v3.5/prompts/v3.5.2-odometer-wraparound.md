# v3.5.2 Odometer Wraparound

Implement the v3.5.2 slice only.

This file is one v3.5 implementation slice.

If invoked from a broad instruction such as "implement the next slice", first confirm this file matches the first unchecked slice in `docs/v3.5/ImplementationState.md`.

Do not implement this slice if an earlier slice is still unchecked.

## Goal

Add odometer wheel-strip wraparound so digits roll cleanly through strip boundaries, especially `9 -> 0`.

## Requirements

- Preserve existing odometer movement semantics.
- Do not change the accepted `odometer.movement` values or fallback behaviour in this slice.
- Add wraparound as an odometer option layered on top of existing behaviour.
- Avoid changing non-odometer gauge semantics.
- Add tests for boundary rollover and disabled/default behaviour.
- Add or update one visual inspection fixture for wraparound.

## Do not

- Do not add easing, carry-drag, backlash, snap/settle, or transition lifecycle in this slice.
- Do not add idle animation.

## Completion

Update `docs/v3.5/ImplementationState.md` to mark v3.5.2 complete and set the next target to v3.5.3.
