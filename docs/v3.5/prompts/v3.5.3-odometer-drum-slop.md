# v3.5.3 Odometer Drum Slop

Implement the v3.5.3 slice only.

This file is one v3.5 implementation slice.

If invoked from a broad instruction such as "implement the next slice", first confirm this file matches the first unchecked slice in `docs/v3.5/ImplementationState.md`.

Do not implement this slice if an earlier slice is still unchecked.

## Goal

Add fixed per-wheel odometer alignment offsets so drums can sit slightly imperfectly while static.

## Requirements

- Treat drum slop as static imperfection, not animation.
- Keep behaviour deterministic.
- Prefer explicit offsets over generated randomness.
- Preserve existing odometer movement semantics.
- Do not change the accepted `odometer.movement` values or fallback behaviour in this slice.
- Add validation for sensible offset bounds.
- Add tests for configured offsets and default no-offset behaviour.
- Add a single-feature visual inspection fixture.

## Do not

- Do not add random runtime offsets.
- Do not add easing, carry-drag, backlash, snap/settle, or transition lifecycle in this slice.

## Completion

Update `docs/v3.5/ImplementationState.md` to mark v3.5.3 complete and set the next target to v3.5.4.
