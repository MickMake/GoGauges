# Codex Prompt: v3.5.6b implement odometer movement model

You are working in the `MickMake/GoDriveLog` repository.

Start from fresh `main` after the v3.5.6a documentation decision has been merged.

Task: implement the documented v3.5.6 odometer movement model.

## Goal

Use `odometer.movement` as the single source of truth for odometer wheel movement.

Do not require or use `realism.movement_policy` for odometer movement.

Supported odometer movement values:

- `instant`: digit display jumps immediately to the target value. No animation.
- `linear`: wheel rolls from old digit position to target digit position at constant speed.
- `ease_out`: wheel starts moving fast, then slows into the target.
- `bell`: wheel starts slow, speeds up through the middle, then slows into the target. Bell-curve velocity / smoothstep-style movement.
- `smooth`: recognised but not implemented. Emit a clear warning and fall back to `instant`.
- `click`: recognised but not implemented. Emit a clear warning and fall back to `instant`.

Unknown odometer movement values must fail configuration loading with a clear error.

`movement` should remain acceptable across gauge types for now. Gauge types without concrete movement behaviour may parse `movement` and keep their current immediate behaviour until their movement slice defines more.

## Required behaviour

When an odometer value changes and movement is animated:

```text
previous digit position -> movement curve -> target digit position
```

At completion, the odometer must settle exactly on the target value.

Do not implement movement by permanently feeding fractional numeric odometer values back into the source display value.

Bad model:

```text
displayValue = 5998.5
```

Good model:

```text
wheel position = movement phase from previous digit position to target digit position
```

## Phase model

Structure odometer movement code so future effects can compose around it:

```text
route -> lead_in -> travel -> settle -> rest
```

This phase model is internal implementation structure, not the public YAML shape. Public config remains feature-oriented. Do not expose `movement.pre`, `movement.primary`, or `movement.post` unless a later docs slice explicitly changes the public config model.

For this slice:

- `route`: existing/default path rules, plus any already-implemented wraparound behaviour;
- `lead_in`: no-op;
- `travel`: controlled by `odometer.movement` using `instant`, `linear`, `ease_out`, or `bell`;
- `settle`: no-op;
- `rest`: existing static offsets such as `drum_slop` may still apply.

Do not implement `carry_drag`, `snap_settle`, or `backlash` in this slice.

## Movement curves

Implement the main movement curves as deterministic bounded functions over progress `t` from 0.0 to 1.0:

- `instant`: position is target immediately.
- `linear`: position progress is `t`.
- `ease_out`: fast start, slow finish.
- `bell`: slow start, fastest middle, slow finish. A smoothstep-style curve is acceptable.

Use named helpers/constants so the curves can be tuned later.

## Fixtures and docs

Update odometer preview YAMLs and examples to use:

```yaml
odometer:
  movement: bell
```

where the intended visual showcase is the bell movement.

Remove `realism.movement_policy` from odometer fixtures/examples.

If an old fixture still uses `smooth` or `click`, it should exercise the warning/fallback behaviour intentionally. Otherwise replace it.

## Tests

Add or update tests covering:

- `movement: instant` jumps directly to the target.
- `movement: linear` animates over ticks and settles exactly on target.
- `movement: ease_out` animates over ticks and settles exactly on target.
- `movement: bell` animates over ticks and settles exactly on target.
- `movement: smooth` emits a warning and behaves as `instant`.
- `movement: click` emits a warning and behaves as `instant`.
- unknown movement values fail configuration loading.
- odometer movement does not require `realism.movement_policy`.
- the completed movement does not retain fractional eased-roll offset as settled state.

Useful odometer value examples:

- `5999 -> 5998` settles exactly on `5998`.
- `5998 -> 5997` settles exactly on `5997`.
- `5990 -> 5989` settles exactly on `5989`.

## Validation

Run the focused dashboard/gauge tests used for this area, including at minimum:

```bash
go test ./internal/dashboard/gauges ./internal/dashboard/v3dashboard
```

If the repo requires the pinned Go binary, use the existing repo convention, for example:

```bash
/usr/local/bin/go1.24.13 test ./internal/dashboard/gauges ./internal/dashboard/v3dashboard
```

Also ensure the preview command builds successfully if that is part of existing validation.

## Scope guard

Do not implement v3.5.7.

Do not implement:

- carry_drag;
- 9-drag;
- snap_settle;
- backlash;
- future stepped click animation;
- future generic smooth animation;
- general physics;
- idle animation;
- asset-root loading changes.

Keep this slice focused on the odometer main movement phase.
