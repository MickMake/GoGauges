# v3.5.11 Radial Peg Bounce

Implement the v3.5.11 slice only.

This file is one v3.5 implementation slice.

If invoked from a broad instruction such as "implement the next slice", first confirm this file matches the first unchecked slice in `docs/v3.5/ImplementationState.md`.

Do not implement this slice if an earlier slice is still unchecked.

## Goal

Add a tiny bounded bounce when a radial needle hits its configured min/max physical stop.

## Requirements

- Trigger only at configured gauge limits.
- Keep the bounce small and deterministic.
- Final settled value must remain clamped to the physical stop.
- Add tests for min stop, max stop, no-stop normal movement, and disabled/default behaviour.
- Add a single-feature visual inspection fixture.

## Do not

- Do not add idle vibration or broader impact physics.
- Do not change radial value mapping semantics.

## Completion

Update `docs/v3.5/ImplementationState.md` to mark v3.5.11 complete and set the next target to v3.5.12.
